# Active Directory Setup

1 Installed Windows Server 2019 as VM on VMWare
Use 'sconfig' to:
    -Change hostname
    -Change IP address to static
    -Change DNS server to self IP address

2 Install AD windows feature
    shell
    Install-WindowsFeature AD-Domain-Services -IncludeManagementTools


2 Installed Windows 10 as a VM on VMWare
    E

